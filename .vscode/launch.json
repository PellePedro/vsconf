{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Worker",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/worker",
            "cwd": "${workspaceFolder}",
            "args": [
                "-log-level",
                "debug",
            ],
            "env": {
                "DEBUGGER": "true"
            }
        }
    ]
}
